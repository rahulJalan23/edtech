import React from 'react';
// import { db } from '../../firebase';
// import { db } from '../../firebase's

const Textbook = () => {
    // console.log(db.folders());
    return (
        <div className="main_content_body">
        </div>
    )
}

export default Textbook
